include ../rate-button/rate-button
include ../image-slider/image-slider
include ../room-info-caption/room-info-caption

mixin room-info({id, images, isShowNavBtns, number, isLux, cost, starscount, commentscount})
  .room-info
    .room-info__wrapper-for-image-slider
      +image-slider({id:id, image_index:1, images:images, isShowNavBtns:isShowNavBtns})
    .room-info__data
      .room-info__wrapper-for-room-info-caption
        +room-info-caption({number:number, isLux:isLux, cost:cost})
      .room-info__feedback
        +rate-button({id:id, count:starscount, disabled: true})
        .room-info__comments
          span.room-info__comments-count #{commentscount}
          span.room-info__comments-text Отзывов