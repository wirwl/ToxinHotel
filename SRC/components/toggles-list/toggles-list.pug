include ../toggle/toggle

mixin toggles-list({caption, toggles=[{text:"Error: items is not defined"}]}={})
  .toggles-list
    h3.toggles-list__caption #{caption}
    ul.toggles-list__list 
      for toggle in toggles
        li.toggles-list__item
          +toggle(toggle)


