mixin masked-input({caption, type, placeholder, value})
  .masked-input    
    if caption
      label.masked-input__caption #{caption}
    input.masked-input__input.js-masked-input__input(type=type placeholder=(placeholder ? placeholder : "ДД.ММ.ГГГГ") value=value) 